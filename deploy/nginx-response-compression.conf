# Include inside the xuheng-aps.com server block.
# Compress public code/styles; do not broaden compression to authentication JSON.
gzip on;
gzip_vary on;
gzip_comp_level 5;
gzip_min_length 1024;
gzip_types text/css application/javascript text/javascript image/svg+xml;
